import CartItems from "@/components/pages/cart/CartItems";
import React from "react";

const CartPage = () => {
  return (
    <section>
      <CartItems />
    </section>
  );
};

export default CartPage;
